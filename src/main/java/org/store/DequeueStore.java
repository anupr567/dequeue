package org.store;

import org.queue.Dequeue;

public class DequeueStore {
int arraySize;
public Dequeue queueObj;
	 public DequeueStore(int arraySize) {   //constructor
		 queueObj = new Dequeue(arraySize);
		 }
		 //instantiating class Dequeue and specifying dequeue array size
}
